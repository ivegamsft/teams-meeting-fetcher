# Example Terraform variables file for Azure deployment
# Copy to terraform.tfvars and fill in your values
# NEVER commit terraform.tfvars with real credentials!

environment      = "dev"
azure_region     = "eastus"
region_short     = "eus"

# Azure Service Principal credentials (get from your Azure admin)
# These values should match the ARM_* environment variables
azure_subscription_id = "<your-subscription-id>"
azure_tenant_id       = "<your-tenant-id>"
azure_client_id       = "<service-principal-client-id>"
azure_client_secret   = "<service-principal-client-secret>"

# Test User Configuration (optional)
# Terraform will automatically generate a random username (using random_pet)
# and a secure password (using random_password) for the test user
create_test_user = true

# Note: Test user requires Domain.Read.All permission on the deployment SPN
# to automatically fetch the default domain. Run scripts/bootstrap-azure-spn.ps1
# or scripts/bootstrap-azure-spn.sh to set up the SPN with all required permissions.
