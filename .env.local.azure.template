# Local overrides for Azure deployments (do not commit)
# Copy to .env.local.azure and fill in values

AZURE_KEYVAULT_URL=<REPLACE_ME>

# Event Grid
EVENTGRID_URI=<REPLACE_ME>
EVENTGRID_KEY=<REPLACE_ME>

# Application Insights
APPINSIGHTS_INSTRUMENTATION_KEY=<REPLACE_ME>

# Required secrets
WEBHOOK_AUTH_SECRET=<REPLACE_ME>
GRAPH_TENANT_ID=<REPLACE_ME>
GRAPH_CLIENT_ID=<REPLACE_ME>
GRAPH_CLIENT_SECRET=<REPLACE_ME>
ENTRA_GROUP_ID=<REPLACE_ME>
