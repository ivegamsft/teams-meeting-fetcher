# Project Context

- **Owner:** ivegamsft
- **Project:** Teams Meeting Fetcher — webhook-driven service for Microsoft Teams meeting transcriptions via Graph API
- **Stack:** Node.js/TypeScript (Express), Python, Terraform, AWS Lambda, Azure Container Apps, Microsoft Graph API, SQLite, DynamoDB, Docker, Jest, GitHub Actions
- **Created:** 2026-02-24

## Team Updates

📌 Team update (2026-02-25T01:30:00Z): EventHub processor Lambda fails with `consumer.subscribe(...).catch is not a function` at handler.js:207. Root cause: @azure/event-hubs returns Subscription object, not Promise. Secondary issue: consumer group mismatch ($Default vs lambda-processor). Needs try/catch refactor and config alignment. — decided by Redfoot

📌 Team update (2026-02-25T01:37:00Z): Fenster fixed the handler.js subscribe() bug by replacing `.catch()` with try/catch wrapper. Also fixed Terraform consumer group wiring in iac/main.tf (uses module.azure output instead of var default). Lambda deployed; Terraform changes pending manual deploy-unified.yml trigger. — decided by Fenster

📌 Team update (2026-02-26T14:56Z): Fenster completed webhook infrastructure: added Lambda env vars `ADMIN_APP_WEBHOOK_URL` and `WEBHOOK_AUTH_SECRET` to Terraform, integrated Secrets Manager, fixed deploy-undefined blockers (no keepers on suffix), and updated deploy-admin-app workflow to set Lambda webhook URL dynamically after IP discovery. Both deployments succeeded. — decided by Fenster

## Learnings

<!-- Append new learnings below. Each entry is something lasting about the project. -->

- Admin app auth now uses Entra ID (Azure AD) OIDC via `passport` + `passport-azure-ad` OIDCStrategy. Login is at `/auth/login`, callback at `/auth/callback`, logout at `/auth/logout`. Session-based via express-session.
- API key auth (`x-api-key` header) is preserved for programmatic/webhook access alongside Entra sessions.
- Auth routes are mounted at app level (`/auth/*`), not under `/api/auth/*`, because the OIDC flow uses browser redirects. The `/api/auth/status` endpoint remains for frontend status checks.
- `passport-azure-ad` is deprecated upstream but still functional. If it breaks, replace with `@azure/msal-node` + custom passport strategy.
- Entra config env vars: `ENTRA_TENANT_ID`, `ENTRA_CLIENT_ID`, `ENTRA_CLIENT_SECRET`, `ENTRA_REDIRECT_URI`. All set by Terraform.
- Meetings pipeline: Graph API sends calendar event changes to EventHub. EventHub Lambda polls events, archives to S3, and optionally forwards to admin app webhook. Admin app webhook route (`/api/webhooks/graph`) calls `meetingService.processNotification()` which fetches event details from Graph API and writes to DynamoDB meetings table.
- Webhook auth uses `WEBHOOK_AUTH_SECRET` (Bearer token) and `WEBHOOK_CLIENT_STATE` (Graph notification validation). Separate from dashboard auth (API key / Entra session).
- The `webhookAuth` middleware is in `src/middleware/auth.ts`. Webhook routes at `/api/webhooks/*` bypass `dashboardAuth` and use `webhookAuth` instead.
- EventHub Lambda env vars for forwarding: `ADMIN_APP_WEBHOOK_URL` (admin app webhook endpoint URL) and `WEBHOOK_AUTH_SECRET` (bearer token). Not yet wired in Terraform -- Fenster needs to add these to `iac/aws/modules/eventhub-processor/`.
- `configStore` tracks webhook activity via `updateLastNotification()` and `updateLastWebhook()` methods (timestamp fields on the config document).
- DynamoDB meetings table uses composite key: `meeting_id` (partition) + `created_at` (sort). The `meetingStore._resolveKey()` does a Query to get both key parts before GetItem.
- E2E pipeline test (2026-02-26): Full pipeline verified end-to-end. Created Graph subscription for a-ivega@ibuyspy.net, created test Teams meetings, confirmed Graph notifications -> EventHub -> Lambda -> S3 archive -> admin app webhook -> DynamoDB. 1 meeting record stored in tmf-meetings-8akfpg.
- Lambda was deployed with placeholder.js (190 bytes) instead of actual handler.js (13.7KB). Root cause: deploy-unified.yml creates placeholder zips for Terraform plan, but the eventhub Lambda code is never redeployed after `terraform apply`. Fixed manually via `aws lambda update-function-code`.
- Admin app runs HTTPS with self-signed TLS certificates (generated during Docker build). The Lambda ADMIN_APP_WEBHOOK_URL must use `https://` not `http://`. Fixed deploy-admin-app.yml to use https in the webhook URL.
- Lambda requires `WEBHOOK_AUTH_SECRET` env var to be non-empty for forwarding to work. If empty/missing, forwarding is silently skipped. When admin app's WEBHOOK_AUTH_SECRET is empty, it accepts all webhook requests (permissive mode).
- Lambda also requires `NODE_TLS_REJECT_UNAUTHORIZED=0` env var when admin app uses self-signed certs, otherwise HTTPS forwarding fails with cert validation errors.
- 4 Graph subscriptions active as of 2026-02-26: user-scoped `/users/{id}/events` with EventHub notification URL. Subscriptions created via app token (client_credentials flow), not delegated user token.
- Monitored users group (2e572630-7b65-470d-82f2-0387ebb04524) currently contains only a-ivega@ibuyspy.net. Other subscriptions exist for boldoriole@ibuyspy.net and two other user IDs.
- deploy-unified.yml gap: after Terraform apply, it rebuilds and redeploys the main Lambda and authorizer, but NOT the eventhub-processor Lambda. The deploy-lambda-eventhub.yml workflow handles this separately but is not triggered by deploy-unified.
- **Sales Blitz Scale Test (2026-02-26):** Created 320 appointments (160 per rep) for trustingboar@ibuyspy.net and boldoriole@ibuyspy.net. Week of Mar 2-6, 2026, 9 AM - 5 PM EST, 15-minute slots. Script: `scripts/sales-blitz-scale-test.py`. Results: 100% success rate (320/320 created), 3 transient network errors auto-recovered via retry logic, 0 HTTP 429 rate limits hit (100ms pacing worked well), 26m 38s duration, 0.20 events/sec average rate. Pipeline processing observed: Lambda consuming batches of 100 notifications every ~15 seconds, S3 EventHub files (187 after 30 minutes), DynamoDB records (303 after 30 minutes, still processing). Pipeline throughput validated at scale. Graph API handled burst load without throttling when properly paced.
- **Admin-App Meeting Event Tracking & Transcription Control (2026-02-26):** Added `changeType` ('created' | 'updated' | 'deleted') and `rawEventData` fields to Meeting model to track webhook event types and preserve full Graph API responses. Updated `meetingService.processNotification()` to capture and store changeType from incoming webhooks. Modified `createMeeting()` to set changeType='created' and store full eventData as rawEventData. Modified `updateMeeting()` to set changeType='updated' and store full eventData. Enhanced `meetingStore.updateStatus()` to accept optional changeType parameter for delete events. Added `getMeetingDetails()` method to fetch fresh online meeting settings from Graph API. Added `toggleTranscription()` method to enable/disable transcription via Graph API PATCH. Added two new routes: GET `/api/meetings/:id/details` and PATCH `/api/meetings/:id/transcription`. Design: Stored full Graph API event response for audit trail; placed new routes in existing meetings.ts; used Graph API fields `isEntryExitAnnounced` and `recordAutomatically` for transcription toggle (may need adjustment based on testing); all endpoints use `dashboardAuth` middleware.
- **Notification-Only Storage Architecture (2026-02-27):** Refactored admin app to store ONLY raw notification data on webhook receipt, NOT auto-fetch full event details from Graph API. Motivation: Isaac directive — "use the raw data and make the meeting details a deliberate action, not automatic. do not want to expose anything that is not in the event." Changes: Added `resource`, `rawNotification`, `detailsFetched` fields to Meeting model. Added `'notification_received'` status type. Replaced `processNotification()` Graph API call with lightweight notification-only storage. Removed `createMeeting()` and `updateMeeting()` — replaced with `fetchDetails()` (single) and `fetchDetailsBatch()` (batch) methods that enrich meetings on-demand. Added POST `/:id/fetch-details` and POST `/batch-fetch-details` routes. Kept `checkForTranscript()` but as deliberate action only (not auto-triggered). Result: Webhook receipt is fast and privacy-preserving (no subject/attendees stored until user fetches). `detailsFetched` flag tracks enrichment state. `rawNotification` always available for audit. `rawEventData` populated separately after fetch.
- **Sales Blitz Full Retest (2026-02-26):** Comprehensive end-to-end pipeline retest with refactored admin app. Script: `scripts/sales-blitz-full-retest.py`. Target week: March 23-27 2026. Results: Phase 1 created 260/260 events (0 failures, 5 transient retries auto-recovered), 23m 7s duration, 0.19 events/sec. Graph API create latency: avg=2853ms, p50=2789ms, p95=3569ms. Phase 2 applied 116 randomized mutations (39 renames, 20 cancellations, 31 reschedules, 26 description changes, 144 untouched), 100% success, 0 rate limits, 7m 5s duration, avg mutation latency ~1400ms, p95=1796ms. Phase 4 verification: DynamoDB total=841 records, 260 with changeType field (confirms notification-only storage architecture is working), S3 archived=282 events. Lambda forwardedCount log entries=0 (log filter may need adjustment or forwarding logs structured differently). Total test duration: 33m 55s. random.seed(42) used for reproducible mutation patterns. Key observation: Graph API create latency ~2.8s average is higher than first blitz (~2.5s), likely due to Teams meeting provisioning overhead. Mutation latency ~1.4s is significantly faster than creates since no meeting room allocation needed.
- **Sales Blitz E2E Retest #2 (2026-02-26, 17:33-18:06 UTC):** Fresh end-to-end test targeting April 6-10, 2026 week. Tests newly deployed processNotification() fix and Lambda forwardWithRetry(). Phase 1: 260/260 events created (0 failures, 1 transient retry auto-recovered), 21m 49s, 0.20 events/sec. Create latency: avg=2939ms, p50=2792ms, p95=3946ms. Phase 2: 116/116 mutations (39 renames, 20 cancellations, 31 reschedules, 26 desc changes), 100% success, 0 rate limits, 7m 1s. Mutation latency: rename avg=1587ms, cancel avg=1565ms, reschedule avg=1623ms, description avg=1509ms. Phase 4 verification: DynamoDB total=1101 (841 prior + 260 new). changeType breakdown: 922 total (868 updated, 54 deleted, 0 created). Status breakdown: 541 scheduled, 480 notification_received, 80 cancelled. S3 total=580 objects (39 new since test start). Lambda forwarded 3,773 notifications total. Lambda retry behavior observed: 9 consecutive forward timeouts at 18:32 UTC (~40s window), then full recovery at 18:33 — both failing invocation and new invocations resumed forwarding. forwardWithRetry() validated. processNotification() fix validated: 0 "created" changeTypes confirms "updated" always overwrites "created" as intended. Total test duration: 32m 35s.
- **S3 Notification Replay (2026-02-27):** Admin app was down during deploy-admin-app workflow (18:27-18:37 UTC) → Lambda couldn't forward "created" notifications → 390 notifications lost from webhook forwarding. Built `scripts/replay-s3-notifications.py` to replay lost "created" events from S3 archive (`s3://tmf-webhooks-eus-dev/eventhub/`). Script: Listed 328 S3 objects (filtered to 36 in time window 18:25-19:00 UTC), extracted 2461 notifications (390 created, 2061 updated, 10 deleted), replayed all 390 "created" notifications to admin app webhook at 200ms pacing, 100% success (0 failures). Result: All 260 DynamoDB records now have changeType="created" (previously 0 created, 246 updated, 14 deleted). Timeline: Script took ~90 seconds to replay 390 notifications. Key learning: S3 archive is the source of truth when webhook forwarding fails; replay strategy works perfectly for recovery. Admin app accepted replayed notifications and updated existing DynamoDB records with correct changeType field.
- **Batch Fetch Meeting Details (2026-02-27):** 480 meetings had `status: 'notification_received'` with empty subjects (notification-only architecture stores raw notification data without auto-fetching from Graph API). Built `scripts/batch-fetch-meeting-details.py` to auto-detect admin app URL from ECS, retrieve API key from Secrets Manager, scan DynamoDB for notification_received meetings, and call `/api/meetings/batch-fetch-details` in batches of 50. Had to set `API_KEY` in Secrets Manager (was empty) and force ECS redeployment to enable API key auth. IP changed from 54.158.104.101 to 54.174.144.75 after redeployment. Results: 480/480 success (0 failures), 10 batches, 133.8s total (2.2 minutes), 0.28s avg per meeting. Before: 480 notification_received, 541 scheduled, 80 cancelled. After: 0 notification_received, 1021 scheduled, 80 cancelled. All meetings now have subjects, organizer info, and rawEventData populated. Graph API handled all 480 fetches without rate limiting (100ms pacing between each). Script is reusable — auto-detects ECS IP and reads API key from Secrets Manager.
- **Change Data Feed Pattern (2026-02-27):** Replaced webhook forwarding (Lambda -> HTTP POST -> admin app -> DynamoDB) with direct DynamoDB writes (Lambda -> DynamoDB). Key files: `apps/aws-lambda-eventhub/handler.js` (new `writeMeetingNotification()`, removed `forwardNotification()`/`forwardWithRetry()`), `iac/aws/modules/eventhub-processor/main.tf` (new `eventhub_meetings` IAM policy + `MEETINGS_TABLE_NAME` env var), `apps/admin-app/src/routes/webhooks.ts` (POST /graph now no-op with deprecation notice). Composite key (meeting_id + created_at) resolved via Query before PutItem, matching meetingStore._resolveKey() pattern. changeType guard preserved: "created" replay won't overwrite "updated"/"deleted". Admin app decoupled — only reads DynamoDB, no longer receives pushes. Webhook endpoint kept for Graph subscription validation handshake. `processNotification()` in meetingService.ts preserved for manual replay. Terraform wires `module.dynamodb.meetings_table_name/arn` into eventhub_processor module. Lambda response stats changed from forwardedCount/forwardRetries/forwardFailures to dynamoWriteCount/dynamoWriteErrors.

- **Graph Permission Grant (2026-02-27):** Audited Teams Meeting Fetcher SPN (63f2f070-e55d-40d3-93f9-f46229544066) appRoleAssignments. Found that OnlineMeetingTranscript.Read.All and OnlineMeetingRecording.Read.All were already granted (Feb 24). Only OnlineMeetings.Read.All was actually missing. Added it via `az ad app permission add` + `az rest` POST to appRoleAssignments endpoint. Also fixed `scripts/grant-graph-permissions.ps1`: corrected OnlineMeetingRecording.Read.All ID from wrong `a4a80d8d-d9a0-4b0a-b5b4-6c0c8e20c2f8` to correct `a4a08342-c95d-476b-b943-97e100569c8d`, corrected OnlineMeetings.ReadWrite.All ID from `88d21fd4-8e5a-4c32-b5e2-4a1c95f34f72` to `b8bb2037-6e08-44ac-a4ea-4674e010e2a4`, and added OnlineMeetings.Read.All (`c1684f21-1984-47fa-9d61-2dc8c296bb70`). All 7 Graph permissions now verified via API: Calendars.ReadWrite, OnlineMeetings.Read.All, OnlineMeetings.ReadWrite.All, OnlineMeetingTranscript.Read.All, OnlineMeetingRecording.Read.All, Group.Read.All, User.Read.All.
- **AzureAD module deprecation:** `az ad app permission add` + `az rest` POST to `/servicePrincipals/{id}/appRoleAssignments` is the modern replacement for the deprecated AzureAD PowerShell module's `New-AzureADServiceAppRoleAssignment`. Use `az rest --method GET --url "https://graph.microsoft.com/v1.0/servicePrincipals/{id}/appRoleAssignments"` to audit current grants. `az ad app permission admin-consent` can timeout but may still succeed server-side — always verify after.

## Cross-Agent Updates

📌 Team update (2026-02-27T02:23:00Z): Kobayashi completed Teams transcription configuration analysis. Critical blockers identified: (1) CsApplicationAccessPolicy missing (403 error on OnlineMeetings API), (2) Graph permissions missing (OnlineMeetings.Read.All, OnlineMeetingTranscript.Read.All, OnlineMeetingRecording.Read.All), (3) Isaac's account (a-ivega@ibuyspy.net) NOT licensed for Teams Premium — only test users (trustingboar, boldoriole) are licensed. Full configuration checklist created with Layer 1-4 breakdown and PowerShell commands. User directive: subscription created for a-ivega is unnecessary. — decided by Kobayashi
