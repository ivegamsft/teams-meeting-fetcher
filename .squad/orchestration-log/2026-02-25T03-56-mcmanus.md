# Orchestration Log: McManus (Backend)

**Timestamp:** 2026-02-25T03:56Z  
**Agent:** McManus (Backend)  
**Requested by:** ivegamsft

## Assignment Reason

User directive: Admin app authentication must use Entra ID (Azure AD). Backend must replace password-based auth with OIDC via passport.

## Mode & Scope

- **Mode:** Spawned as background agent
- **Authorization:** apps/admin-app code scope

## Work Summary

**Replaced password auth with Entra OIDC in admin app:**
- Added `passport` + `passport-azure-ad` dependencies to package.json
- Created `src/middleware/entraAuth.ts` — new passport OIDC strategy configuration
- Updated `src/config/index.ts` — added `entra` config section, removed `dashboardPassword`
- Updated `src/middleware/auth.ts` — checks `req.isAuthenticated()` via passport
- Replaced password login with OIDC login/callback/logout in `src/routes/auth.ts`
- Updated `src/app.ts` — added passport init, mounted `/auth` routes
- Updated `src/routes/index.ts` — renamed import (no functional change)
- Updated `public/index.html` — removed login form, added user info display in sidebar
- Updated `public/js/app.js` — redirect to `/auth/login` instead of password form
- Updated `public/js/api.js` — removed password login/logout methods
- Updated `.env.example` — added Entra vars, removed DASHBOARD_PASSWORD
- Updated `.env.development` — removed DASHBOARD_PASSWORD

**API & webhook auth preserved:**
- API key (`x-api-key` header) still works for programmatic access
- Webhook bearer token auth unchanged
- Health check (`/health`) requires no auth

## Outcome

**Status:** ✅ Completed  
**Validation:** TypeScript compiles clean  
**Dependencies:** Fenster's Terraform entra config now connected to backend routes
