# Local overrides (do not commit)
# Copy to .env.local and fill in values as needed

# AWS Integration (if using Lambda)
AWS_WEBHOOK_ENDPOINT=<REPLACE_ME>
AWS_S3_BUCKET=<REPLACE_ME>
AWS_API_KEY=<REPLACE_ME>

# Graph API
GRAPH_TENANT_ID=<REPLACE_ME>
GRAPH_CLIENT_ID=<REPLACE_ME>
GRAPH_CLIENT_SECRET=<REPLACE_ME>
ENTRA_GROUP_ID=<REPLACE_ME>

# Webhook auth
WEBHOOK_AUTH_SECRET=<REPLACE_ME>

# Optional: Event Grid
EVENTGRID_URI=<REPLACE_ME>
EVENTGRID_KEY=<REPLACE_ME>
